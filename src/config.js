/**
 * Проверка прав администратора.
 * Single-user режим: приложение работает один пользователь, поэтому
 * все действия (очистка хранилища и т.п.) доступны всегда.
 */
export function isAdmin() {
  return true
}
